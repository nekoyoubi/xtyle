{
	"$schema": "https://xript.dev/schema/mod/v0.7.json",
	"xript": "0.7",
	"name": "xtyle-accordion-default",
	"version": "0.0.1",
	"title": "Xtyle Accordion (default)",
	"description": "The built-in fill for the xtyle Accordion component: the mod-zero markup and behavior every app inherits and may override.",
	"capabilities": ["xtyle.component.accordion"],
	"entry": { "script": "mod.js", "format": "script" },
	"fills": {
		"component.accordion": [
			{
				"id": "accordion",
				"format": "text/html+jsml",
				"source": "accordion.html",
				"handlers": [
					{ "selector": ".xtyle-accordion__trigger", "on": "click", "handler": "toggleSection" },
					{ "selector": ".xtyle-accordion__trigger", "on": "keydown", "handler": "navKeydown" }
				]
			}
		]
	}
}
