// generated by scripts/build-fragments.mjs: do not edit
export const manifest = {"$schema":"https://xript.dev/schema/mod/v0.7.json","xript":"0.7","name":"xtyle-tree-default","version":"0.0.1","title":"Xtyle Tree (default)","description":"The built-in fill for the xtyle Tree component: the mod-zero markup and behavior every app inherits and may override.","capabilities":["xtyle.component.tree"],"entry":{"script":"mod.js","format":"script"},"fills":{"component.tree":[{"id":"tree","format":"text/html+jsml","source":"tree.html","handlers":[{"selector":".xtyle-tree__action","on":"click","handler":"tree__rowAction"},{"selector":".xtyle-tree__twisty:not(.xtyle-tree__twisty--leaf)","on":"click","handler":"tree__toggleTwisty"},{"selector":".xtyle-tree__row","on":"click","handler":"tree__selectRow"},{"selector":"[role=\"treeitem\"]","on":"keydown","handler":"tree__navKeydown"}],"meta":{"tokens":["--font-sans","--text-sm","--text-body","--fg-0","--fg-1","--fg-2","--fg-3","--fg-disabled","--space-0","--space-1","--space-2","--space-4","--text-xs","--radius-sm","--duration-fast","--ease-standard","--state-hover","--border-normal","--border-thick","--ring","--accent-bg","--accent-text","--weight-medium","--selection-cue"]}}]}};
export const fragmentSources: Record<string, string> = {"mod.js":"\"use strict\";\n(() => {\n  // packages/xtyle/src/elements/fragments/escape.ts\n  var AMP = /&/g;\n  var LT = /</g;\n  var GT = />/g;\n  var DQUOTE = /\"/g;\n  var SQUOTE = /'/g;\n  function escapeHtml(value) {\n    return value.replace(AMP, \"&amp;\").replace(LT, \"&lt;\").replace(GT, \"&gt;\");\n  }\n  function escapeAttr(value) {\n    return escapeHtml(value).replace(DQUOTE, \"&quot;\").replace(SQUOTE, \"&#39;\");\n  }\n\n  // packages/xtyle/src/elements/fragments/selector-escape.ts\n  var BACKSLASH = /\\\\/g;\n  var DQUOTE2 = /\"/g;\n  var NEWLINE = /\\n/g;\n  var CR = /\\r/g;\n  function escapeSelectorValue(value) {\n    return value.replace(BACKSLASH, \"\\\\\\\\\").replace(DQUOTE2, '\\\\\"').replace(NEWLINE, \"\\\\A \").replace(CR, \"\\\\D \");\n  }\n\n  // packages/xtyle/src/elements/collection/roving.ts\n  function focusableAt(items, index) {\n    const item = items[index];\n    return item && !item.skip ? item.key : null;\n  }\n  function firstKey(items) {\n    for (let i = 0; i < items.length; i++) {\n      const key = focusableAt(items, i);\n      if (key !== null) return key;\n    }\n    return null;\n  }\n  function lastKey(items) {\n    for (let i = items.length - 1; i >= 0; i--) {\n      const key = focusableAt(items, i);\n      if (key !== null) return key;\n    }\n    return null;\n  }\n  function stepKey(items, fromKey, dir, wrap = false) {\n    const n = items.length;\n    if (n === 0) return null;\n    const found = fromKey === null ? -1 : items.findIndex((it) => it.key === fromKey);\n    const here = found === -1 ? dir > 0 ? -1 : n : found;\n    for (let s = 1; s <= n; s++) {\n      let idx = here + dir * s;\n      if (wrap) idx = (idx % n + n) % n;\n      else if (idx < 0 || idx >= n) return null;\n      const key = focusableAt(items, idx);\n      if (key !== null) return key;\n      if (wrap && idx === here) break;\n    }\n    return null;\n  }\n\n  // packages/xtyle/src/elements/collection/nav-reducer.ts\n  function axisKeys(orientation) {\n    switch (orientation) {\n      case \"horizontal\":\n        return { next: [\"ArrowRight\"], prev: [\"ArrowLeft\"] };\n      case \"both\":\n        return { next: [\"ArrowDown\", \"ArrowRight\"], prev: [\"ArrowUp\", \"ArrowLeft\"] };\n      default:\n        return { next: [\"ArrowDown\"], prev: [\"ArrowUp\"] };\n    }\n  }\n  function linearNav(items, currentKey, key, opts = {}) {\n    const orientation = opts.orientation ?? \"vertical\";\n    const wrap = opts.wrap ?? false;\n    const { next, prev } = axisKeys(orientation);\n    if (next.includes(key)) {\n      const target = stepKey(items, currentKey, 1, wrap);\n      return target !== null ? { focus: target, handled: true } : { handled: true };\n    }\n    if (prev.includes(key)) {\n      const target = stepKey(items, currentKey, -1, wrap);\n      return target !== null ? { focus: target, handled: true } : { handled: true };\n    }\n    if (opts.homeEnd && key === \"Home\") {\n      const target = firstKey(items);\n      return target !== null ? { focus: target, handled: true } : { handled: true };\n    }\n    if (opts.homeEnd && key === \"End\") {\n      const target = lastKey(items);\n      return target !== null ? { focus: target, handled: true } : { handled: true };\n    }\n    if (key === \"Enter\" || key === \" \" || key === \"Spacebar\") {\n      return { activate: true, handled: true };\n    }\n    return {};\n  }\n\n  // packages/xtyle/src/elements/fragments/tree/mod.ts\n  function treeClass(bindings) {\n    const size = bindings.size ?? \"md\";\n    return size === \"md\" ? \"xtyle-tree\" : `xtyle-tree xtyle-tree--${size}`;\n  }\n  function nodeKey(node) {\n    return node.value ?? node.label;\n  }\n  function isExpanded(node, hasChildren, expanded) {\n    const locked = (node.locked ?? false) && hasChildren;\n    if (locked) return true;\n    return expanded.has(nodeKey(node));\n  }\n  function isStaticNode(node) {\n    const hasChildren = !!(node.children && node.children.length);\n    const locked = (node.locked ?? false) && hasChildren;\n    return locked && !node.href;\n  }\n  function firstFocusableKey(nodes) {\n    for (const node of nodes) {\n      if (!isStaticNode(node)) return nodeKey(node);\n      if (node.children && node.children.length) {\n        const child = firstFocusableKey(node.children);\n        if (child) return child;\n      }\n    }\n    return null;\n  }\n  function rovingTarget(bindings) {\n    if (bindings.rovingValue) return bindings.rovingValue;\n    if (bindings.selectedValue) return bindings.selectedValue;\n    return firstFocusableKey(bindings.items ?? []);\n  }\n  function treeBadges(badge) {\n    if (badge == null) return [];\n    const list = Array.isArray(badge) ? badge : [badge];\n    return list.map((b) => typeof b === \"string\" ? { text: b } : b);\n  }\n  function treeTrailing(node, value, isLink) {\n    const badges = treeBadges(node.badge).map(\n      (b) => `<span class=\"xtyle-tree__badge${b.tone ? ` xtyle-tree__badge--${b.tone}` : \"\"}\" part=\"badge\" aria-hidden=\"true\">${escapeHtml(b.text)}</span>`\n    ).join(\"\");\n    const actionItems = !isLink && node.actions ? node.actions : [];\n    const actions = actionItems.length ? `<span class=\"xtyle-tree__actions\" part=\"actions\">${actionItems.map((a) => {\n      const disabled = a.disabled ? ` disabled data-disabled=\"true\"` : \"\";\n      return `<button type=\"button\" class=\"xtyle-tree__action\" part=\"row-action\" data-action=\"${escapeAttr(a.id)}\" data-value=\"${escapeAttr(value)}\" aria-label=\"${escapeAttr(a.label)}\" title=\"${escapeAttr(a.label)}\" tabindex=\"-1\"${disabled}>${escapeHtml(a.icon ?? a.label)}</button>`;\n    }).join(\"\")}</span>` : \"\";\n    return badges || actions ? `<span class=\"xtyle-tree__trailing\">${badges}${actions}</span>` : \"\";\n  }\n  function buildNodes(nodes, level, selectedValue, expanded, roving) {\n    return nodes.map((node) => {\n      const hasChildren = !!(node.children && node.children.length);\n      const value = nodeKey(node);\n      const locked = (node.locked ?? false) && hasChildren;\n      const open = isExpanded(node, hasChildren, expanded);\n      const selected = value === selectedValue;\n      const disabled = node.disabled ?? false;\n      const disabledData = disabled ? ` data-disabled=\"true\"` : \"\";\n      const twisty = hasChildren && !locked ? `<span class=\"xtyle-tree__twisty\" aria-hidden=\"true\" data-value=\"${escapeAttr(value)}\"${disabledData}><svg viewBox=\"0 0 24 24\"><path fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" d=\"M9 6l6 6-6 6\" /></svg></span>` : `<span class=\"xtyle-tree__twisty xtyle-tree__twisty--leaf\" aria-hidden=\"true\"></span>`;\n      const label = `<span class=\"xtyle-tree__label\">${escapeHtml(node.label)}</span>`;\n      const isLink = !!node.href;\n      const isStatic = locked && !isLink;\n      const staticData = isStatic ? ` data-static=\"true\"` : \"\";\n      const rowClass = isStatic ? \"xtyle-tree__row xtyle-tree__row--static\" : \"xtyle-tree__row\";\n      const titleAttr = node.title ? ` title=\"${escapeAttr(node.title)}\"` : \"\";\n      const rowOpen = isLink ? `<a class=\"xtyle-tree__row\" part=\"row\" href=\"${escapeAttr(node.href)}\" tabindex=\"-1\" data-value=\"${escapeAttr(value)}\"${disabledData}${titleAttr} style=\"--tree-level: ${level}\">` : `<div class=\"${rowClass}\" part=\"row\" data-value=\"${escapeAttr(value)}\"${disabledData}${staticData}${titleAttr} style=\"--tree-level: ${level}\">`;\n      const rowClose = isLink ? \"</a>\" : \"</div>\";\n      const trailing = treeTrailing(node, value, isLink);\n      const group = hasChildren ? `<ul class=\"xtyle-tree__group\" role=\"group\"${open ? \"\" : \" hidden\"}>${buildNodes(node.children, level + 1, selectedValue, expanded, roving)}</ul>` : \"\";\n      const expandedAttr = hasChildren ? ` aria-expanded=\"${String(open)}\"` : \"\";\n      const disabledAttr = disabled ? ` aria-disabled=\"true\"` : \"\";\n      const lockedAttr = locked ? ` data-locked=\"true\"` : \"\";\n      const itemClass = locked ? \"xtyle-tree__item xtyle-tree__item--locked\" : \"xtyle-tree__item\";\n      const tabindex = !isStatic && value === roving ? \"0\" : \"-1\";\n      return `<li class=\"${itemClass}\" role=\"treeitem\"${expandedAttr} aria-selected=\"${escapeAttr(String(selected))}\"${disabledAttr}${lockedAttr} aria-level=\"${level}\" data-value=\"${escapeAttr(value)}\" tabindex=\"${tabindex}\">${rowOpen}${twisty}${label}${trailing}${rowClose}${group}</li>`;\n    }).join(\"\");\n  }\n  function tree(bindings) {\n    const items = bindings.items ?? [];\n    const expanded = new Set(bindings.expandedKeys ?? []);\n    const selected = bindings.selectedValue ?? null;\n    const roving = rovingTarget(bindings);\n    return buildNodes(items, 1, selected, expanded, roving);\n  }\n  hooks.fragment.mount(\"tree\", (bindings, ops) => {\n    ops.setAttr(\".xtyle-tree\", \"class\", treeClass(bindings));\n    if (bindings.labelledby) ops.setAttr(\"[data-root]\", \"aria-labelledby\", bindings.labelledby);\n    else if (bindings.label) ops.setAttr(\"[data-root]\", \"aria-label\", bindings.label);\n    ops.replaceChildren(\"[data-root]\", tree(bindings));\n  });\n  hooks.fragment.update(\"tree\", (bindings, ops) => {\n    ops.setAttr(\".xtyle-tree\", \"class\", treeClass(bindings));\n    const expanded = new Set(bindings.expandedKeys ?? []);\n    const selected = bindings.selectedValue ?? null;\n    const roving = rovingTarget(bindings);\n    const walk = (nodes) => {\n      for (const node of nodes) {\n        const hasChildren = !!(node.children && node.children.length);\n        const value = nodeKey(node);\n        const sel = `[role=\"treeitem\"][data-value=\"${escapeSelectorValue(value)}\"]`;\n        ops.setAttr(sel, \"aria-selected\", String(value === selected));\n        ops.setAttr(sel, \"tabindex\", value === roving ? \"0\" : \"-1\");\n        if (hasChildren) {\n          const open = isExpanded(node, hasChildren, expanded);\n          const locked = (node.locked ?? false) && hasChildren;\n          if (!locked) ops.setAttr(sel, \"aria-expanded\", String(open));\n          ops.toggle(`${sel} > .xtyle-tree__group`, open);\n          walk(node.children);\n        }\n      }\n    };\n    walk(bindings.items ?? []);\n  });\n  xript.exports.register(\"tree__selectRow\", (payload) => {\n    const e = payload;\n    if (e.dataset?.disabled === \"true\" || e.dataset?.static === \"true\") return {};\n    const key = e.dataset?.value;\n    if (!key) return {};\n    const isLink = e.tagName === \"A\";\n    if (isLink) return { select: key, focus: key };\n    return { select: key, focus: key, expandKey: key };\n  });\n  xript.exports.register(\"tree__rowAction\", (payload) => {\n    const e = payload;\n    if (e.dataset?.disabled === \"true\") return { stopPropagation: true, preventDefault: true };\n    const action = e.dataset?.action;\n    const value = e.dataset?.value;\n    if (!action || value === void 0) return { stopPropagation: true };\n    return { emit: { type: \"tree-action\", detail: { value, action } }, stopPropagation: true, preventDefault: true };\n  });\n  xript.exports.register(\"tree__toggleTwisty\", (payload) => {\n    const e = payload;\n    if (e.dataset?.disabled === \"true\") return {};\n    const key = e.dataset?.value;\n    if (!key) return {};\n    return { focus: key, expandKey: key, preventDefault: true, stopPropagation: true };\n  });\n  xript.exports.register(\"tree__navKeydown\", (payload, context) => {\n    const e = payload;\n    const ctx = context;\n    const k = e.key ?? \"\";\n    const current = e.dataset?.value ?? \"\";\n    const rows = ctx.rows ?? [];\n    const here = rows.findIndex((r) => r.key === current);\n    const row = here >= 0 ? rows[here] : void 0;\n    if (!row) return {};\n    const isStatic = (r) => r.locked && !r.isLink;\n    const navItems = rows.map((r) => ({ key: r.key, skip: isStatic(r) }));\n    switch (k) {\n      case \"ArrowRight\": {\n        if (row.expandable && !row.expanded)\n          return { expandKey: row.key, expand: true, focus: row.key, preventDefault: true, stopPropagation: true };\n        if (row.expandable && row.expanded) {\n          const child = rows.find((r) => r.parent === row.key && !isStatic(r));\n          return child ? { focus: child.key, preventDefault: true, stopPropagation: true } : { preventDefault: true, stopPropagation: true };\n        }\n        return { preventDefault: true, stopPropagation: true };\n      }\n      case \"ArrowLeft\": {\n        if (row.expandable && row.expanded && !row.locked)\n          return { expandKey: row.key, expand: false, focus: row.key, preventDefault: true, stopPropagation: true };\n        if (row.parent !== null) {\n          const parent = rows.find((r) => r.key === row.parent);\n          if (parent && !isStatic(parent)) return { focus: row.parent, preventDefault: true, stopPropagation: true };\n          const above = stepKey(navItems, row.parent, -1);\n          if (above !== null) return { focus: above, preventDefault: true, stopPropagation: true };\n        }\n        return { preventDefault: true, stopPropagation: true };\n      }\n      case \"Enter\":\n      case \" \":\n      case \"Spacebar\": {\n        if (row.disabled || isStatic(row)) return { preventDefault: true, stopPropagation: true };\n        if (row.isLink) return { select: row.key, activate: row.key, preventDefault: true, stopPropagation: true };\n        if (row.expandable) return { select: row.key, expandKey: row.key, preventDefault: true, stopPropagation: true };\n        return { select: row.key, preventDefault: true, stopPropagation: true };\n      }\n    }\n    const move = linearNav(navItems, current, k, { orientation: \"vertical\", homeEnd: true });\n    if (move.handled) {\n      return move.focus !== void 0 ? { focus: move.focus, preventDefault: true, stopPropagation: true } : { preventDefault: true, stopPropagation: true };\n    }\n    return { stopPropagation: true };\n  });\n})();\n","tree.html":"<ul class=\"xtyle-tree\" part=\"tree\" role=\"tree\" data-root></ul>\n"};
