{
	"$schema": "https://xript.dev/schema/mod/v0.7.json",
	"xript": "0.7",
	"name": "xtyle-select-default",
	"version": "0.0.1",
	"title": "Xtyle Select (default)",
	"description": "The built-in fill for the xtyle Select component: the mod-zero markup and behavior every app inherits and may override.",
	"capabilities": ["xtyle.component.select"],
	"entry": { "script": "mod.js", "format": "script" },
	"fills": {
		"component.select": [
			{
				"id": "select",
				"format": "text/html+jsml",
				"source": "select.html"
			}
		]
	}
}
