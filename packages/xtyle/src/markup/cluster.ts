/** The host-layout rules for a cluster — the `:host` rules, shared by the element's scaffold and the SSR declarative shadow root. */
export const clusterHostCss = ":host { display: block; } :host([inline]) { display: inline-block; }";
