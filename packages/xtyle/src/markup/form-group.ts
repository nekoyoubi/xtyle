/** The host-layout rule for a form group — the one `:host` rule, shared by the element's scaffold and the SSR declarative shadow root. */
export const formGroupHostCss = ":host { display: block; }";
