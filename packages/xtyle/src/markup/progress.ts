/** The host-layout rules for a progress control — shared by the element's scaffold and the SSR declarative shadow root. */
export const progressHostCss = `:host { display: inline-flex; vertical-align: middle; } :host([variant="linear"]) { display: flex; width: 100%; }`;
