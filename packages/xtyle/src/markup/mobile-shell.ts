/** The host-layout rule for the mobile shell: the one `:host` rule, shared by the element's scaffold and the SSR declarative shadow root. */
export const mobileShellHostCss = ":host { display: contents; }";
