/** The host-layout rules for a stack — the `:host` rules, shared by the element's scaffold and the SSR declarative shadow root. */
export const stackHostCss = ":host { display: block; } :host([inline]) { display: inline-block; }";
